# @package _global_
defaults:
  - override /training: galaxy10-training
  - override /model: eq_nasnet
  - override /wandb: default
  - _self_
  
  
model:
  blocks_args_dict:
    _3:
      se_ratio: 0.0
  dropout_rate: 0.0

wandb:
  give_name: final_isic2019
  notes: droout0.0_se0.0

# https://hydra.cc/docs/patterns/configuring_experiments/
# I don't know how to attach deep config groups