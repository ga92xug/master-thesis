DeepDRiD:
  filters:
    config.training.adversarial_attack: Foolbox
    config.training.epochs: 2 # full training
    config.training.dataset.name: DeepDRiD_quality
Blood:
  filters:
    config.training.adversarial_attack: Foolbox
    config.training.epochs: 100 # full training
    config.training.dataset.name: blood
