isic2019:
  filters:
    tags: CNN_augmentation_isic2019
    config.training.earlystop.stop: false

DeepDRiD:
  filters:
    tags: CNN_augmentation_DeepDRiD
    config.training.earlystop.stop: false
    #config.training.earlystop.monitor: valid.loss
    #config.training.dataset._target_: "training.datasets.general.data_loader.get_DeepDRiD" 
    # created_at:
    #     $gt: '2023-11-03###'
    # intial experiments have been with valid.acc but more variance accross all models
blood:
  filters:
    tags: CNN_augmentation_blood
    config.training.earlystop.stop: false
    #created_at:
    #    $gt: '2023-11-06###'

wandb_projects: ["SL-Application"]