# @package _global_
defaults:
  - imagenet_100
  - override /train: imagenette
  - override /train/network: eq_nasnet 

tags: ["pre-train_imagenette_50"]
test_mode: test

train:
  trainer:
    max_epochs: 50
