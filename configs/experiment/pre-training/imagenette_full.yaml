# @package _global_
defaults:
  - override /train: imagenet
  - override /train/network: eq_nasnet 
  - override /train/network/blocks_args_dict: 4blocks

tags: ["pre-train_imagenette_50"]
seed: 0
logger:
  wandb:
    project: "pre-train"

train:
  trainer:
    max_epochs: 50
