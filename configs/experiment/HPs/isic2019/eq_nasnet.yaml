# @package _global_
defaults:
  - HPs/isic2019/all
  - override /train/network: eq_nasnet
  - override /train/scheduler: multi_step_lr

train:
  optimizer:
    lr: 0.005
    weight_decay: 1e-5
  scheduler:
    milestones: [35,75,90,105]
    gamma: 0.5
