# @package _global_
defaults:
  - HPs/deepdrip/all
  - override /train/network: vit_pre
  - override /train/scheduler: reduce_lr_on_plateau

train:
  optimizer:
    lr: 0.00005
    weight_decay: 0.0000197902789003467
  scheduler: 
    patience: 5
    factor: 0.06938868866678677
  trainer:
    log_every_n_steps: 1

