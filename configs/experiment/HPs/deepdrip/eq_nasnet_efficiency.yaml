# @package _global_
defaults:
  - HPs/deepdrip/all
  - override /train/network: eq_nasnet

train:
  network:
    dropout_rate: 0.2
  optimizer:
    lr: 0.0013960833048305068
    weight_decay: 0.000001
