# @package _global_
defaults:
  - HPs/deepdrip/all
  - override /train/network: efficientnet_pre
  - override /train/scheduler: none

train:
  network:
    pre_trained: True
  optimizer:
    lr: 0.00005
    weight_decay: 0.001


