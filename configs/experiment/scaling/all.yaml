# @package _global_
defaults:
  - override /train: isic2019-constant_scheduler
  
train:
  dataset:
    batch_size: 64

    resolution: 96
  
wandb:
  project: SL-Scaling
  give_name: final_isic2019
  notes: change_skip
  tags: [change_skip]