# @package _global_
defaults:
  - override /training_setup: cifar10-training
  - override /training_setup.model: eq_wrn
  - override /wandb: default
  
  
model:
  fix_params_mode: "heuristic" 
  restrict: [halved, invariant]
  group: cyclic 
  rotation: 8
  

logger:
  wandb:
    project: "SL-inital"

    tags: [ablation_nas_architecture, change_conv]

