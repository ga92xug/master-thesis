# @package _global_
defaults:
  - override /training_setup: blood-training
  - override /model: efficientnet
  - override /wandb: default
  - _self_
  
model:
  pretrained: False


training_setup:
  optimizer:
    lr: 0.0005258641063603519
    weight_decay: 0.00000165943876390034

  scheduler:
    _target_: null
    #factor: 0.5
    #patience: 10
    #verbose: True

logger:
  wandb:
    project: SL-Application
    name: True
    tags: []

