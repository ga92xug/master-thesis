# @package _global_
defaults:
  - override /train: camelyon17
  - override /train/network: eq_nasnet
  - override /train/scheduler: cosine_annealing_lr
  - override /logger: wandb

task_name: camelyon17_domain_shift
tags: ["tuned_HPs"]

logger:
  wandb:
    project: domain_shift

train:
  optimizer:
    lr: 0.05507
    weight_decay: 3.986e-6