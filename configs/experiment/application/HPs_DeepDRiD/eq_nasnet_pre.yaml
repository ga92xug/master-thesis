# @package _global_
defaults:
  - override /train: deepdrip
  - override /train/network: eq_nasnet_pre
  - override /train/scheduler: cosine_annealing_lr
  - override /logger: wandb

task_name: DeepDRiD_domain_shift
test_mode: test
tags: ["DeepDRiD_low_data","tuned_HPs"]

logger:
  wandb:
    project: low_data

train:
  optimizer:
    lr: 0.1
    weight_decay: 1.23e-5
