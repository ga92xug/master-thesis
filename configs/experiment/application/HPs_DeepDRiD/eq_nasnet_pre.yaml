# @package _global_
defaults:
  - override /train: deepdrip
  - override /train/network: eq_nasnet_pre
  - override /train/scheduler: none
  - override /logger: wandb

train:
  optimizer:
    lr: 0.006984 # 0.1
    weight_decay: 6e-5 # 1.23e-5

task_name: deepdrip_low_data
tags: ["tuned_HPs"]

logger:
  wandb:
    project: low_data
