# @package _global_
defaults:
  - override /training_setup: DeepDRiD-training
  - override /training_setup/dataset: DeepDRiD
  - override /training_setup/network: vit #eq_nasnet
  - override /training_setup/scheduler: none
  - override /logger: wandb
  - override /training_setup/callbacks: default 
  - _self_



tags: ["redo_vit_DeepDRiD_res128"]

logger:
  wandb:
    project: master_thesis_important
    mode: online

training_setup:
  network:
    pretrained: False
  optimizer:
    lr: 0.00005
    weight_decay: 0.0001803093201705675

  scheduler: # not used -> implement None scheduler
    _target_: null
    # patience: 43
    # factor: 0.1680463774997989

  

