# @package _global_
defaults:
  - override /train: DeepDRiD-training
  - override /train/dataset: DeepDRiD
  - override /train/network: vit #eq_nasnet
  - override /train/scheduler: none
  - override /logger: wandb
  - override /train/callbacks: default 
  - _self_



tags: ["redo_vit_DeepDRiD_res128"]

logger:
  wandb:
    project: master_thesis_important
    mode: online

train:
  network:
    pretrained: False
  optimizer:
    lr: 0.00005
    weight_decay: 0.0001803093201705675

  scheduler: # not used -> implement None scheduler
    _target_: null
    # patience: 43
    # factor: 0.1680463774997989

  

