defaults:
  - _self_
  - dataset: blood
  - callbacks: default
  - network: eq_nasnet
  - optimizer: Adam
  - scheduler: reduce_lr_on_plateau

callbacks:
  model_checkpoint:
    monitor: "valid/loss"
    mode: "min"
    save_last: True
    auto_insert_metric_name: False

  early_stopping:
    monitor: "valid/loss"
    patience: 15
    mode: "min"

scheduler: 
  factor: 0.5
  patience: 10

optimizer:
  lr: 0.005
  weight_decay: 1e-5

trainer:
  accumulate_grad_batches: 1
  max_epochs: 100

dataset:
  batch_size: 128                





