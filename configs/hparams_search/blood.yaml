# @package _global_
defaults:
  - default
  - override /train: blood

optimized_metric: "valid/loss"

hydra:
  sweeper:
    ax_config:
      early_stop:
        minimize: true
      experiment:
        name: blood_HPO
        objective_name: ${optimized_metric}
        minimize: true


