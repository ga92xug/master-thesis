# reasons you might want to use `environment.yaml` instead of `requirements.txt`:
# - pip installs packages in a loop, without ensuring dependencies across all packages
#   are fulfilled simultaneously, but conda achieves proper dependency control across
#   all packages
# - conda allows for installing packages without requiring certain compilers or
#   libraries to be available in the system, since it installs precompiled binaries

name: minimal_requirements

channels:
  - pytorch
  - conda-forge
  - defaults

# it is strongly recommended to specify versions of packages installed through conda
# to avoid situation when version-unspecified packages install their latest major
# versions which can sometimes break things
# current approach below keeps the dependencies in the same major versions across all
# users, but allows for different minor and patch versions of packages where backwards
# compatibility is usually guaranteed

# pyrootutils

dependencies:
  #- python=3.10
  #- pytorch=2.1
  #- pytorch-cuda=11.7
  #- torchvision=0.16
  #- torchaudio=2.1
  #- lightning=2.1
  #- torchmetrics=0.11
  - hydra-core=1.3
  - rich=13.7
  #- pre-commit=3.*
  - einops
  - iopath
  - pytest=7.4
  - wandb
  - ax-platform
  - foolbox
  - fvcore
  - h5py
  - opt_einsum
  - pillow
  - scikit-image
  - scikit-learn

  # --------- loggers --------- #
  # - wandb
  # - neptune-client
  # - mlflow
  # - comet-ml
  # - aim>=3.16.2 # no lower than 3.16.2, see https://github.com/aimhubio/aim/issues/2550

  - pip>=23
  - pip:
      - hydra-optuna-sweeper
      - hydra-colorlog
      - rootutils


# conda create -n minimal_requirements python=3.10 pytorch torchvision torchaudio lightning  pytorch-cuda=11.7 -c pytorch -c nvidia
# conda env update --file specs.yaml
# might not be needed on some systems:
# conda install -c conda-forge libstdcxx-ng=12 