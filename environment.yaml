# conda create -n scaling python=3.10 pytorch torchvision torchaudio lightning  pytorch-cuda=11.7 -c pytorch -c nvidia
# conda env update --file environment.yaml

#name: scaling

channels:
  - pytorch
  - conda-forge
  - defaults
  - nvidia

dependencies:
  #- python=3.10
  #- pytorch=2.1
  #- pytorch-cuda=11.7
  #- torchvision=0.16
  #- torchaudio=2.1
  #- lightning=2.1
  #- torchmetrics=0.11
  - hydra-core=1.3
  #- rich=13.7
  #- pre-commit=3.*
  - einops
  - iopath
  - pytest=7.4
  - wandb
  - ax-platform
  - foolbox
  - fvcore
  - h5py
  - opt_einsum
  - pillow
  - scikit-image
  - scikit-learn

  - pip>=23
  - pip:
      - hydra-optuna-sweeper
      - hydra-colorlog
      - rootutils


# might not be needed on some systems:
# conda install -c conda-forge libstdcxx-ng=12 